import MySQL from "../mysql";

/*  export default class Insert {

    public conexion: MySQL;

    constructor () {
        this.conexion.cnn.query('select * from prueba', (err, rows) => {
            if(err) throw err;
            console.log("Data");
            console.log(rows);
        });
    }
} */
